generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}
modal contactos{
 id        String   @id @default(uuid())
 name      String
 
}